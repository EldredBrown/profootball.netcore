@model GameListViewModel
@{
    ViewData["Title"] = "Games";
}

<h1>Games</h1>

<form>
    <p>
        <label class="col-form-label">Season</label>
        @Html.DropDownList("Seasons", null, htmlAttributes: new
        {
            @onchange = "document.location.href='/Games/SetSelectedSeasonId?SeasonId=' + this.options[this.selectedIndex].value;"
        })
    </p>
</form>

<form>
    <p>
        <label class="col-form-label">Week</label>
        @Html.DropDownList("Weeks", null, string.Empty, htmlAttributes: new
        {
            @onchange = "document.location.href='/Games/SetSelectedWeek?WeekId=' + this.options[this.selectedIndex].value;"
        })
    </p>
</form>

<p>
    <a asp-action="Create" class="btn btn-primary">Add Game</a>
</p>
<table class="table table-striped">
    <thead>
        <tr>
            <th>
                <label asp-for="@Model.Games.FirstOrDefault().SeasonId"></label>
            </th>
            <th>
                <label asp-for="@Model.Games.FirstOrDefault().Week"></label>
            </th>
            <th>
                <label asp-for="@Model.Games.FirstOrDefault().GuestName"></label>
            </th>
            <th>
                <label asp-for="@Model.Games.FirstOrDefault().GuestScore"></label>
            </th>
            <th>
                <label asp-for="@Model.Games.FirstOrDefault().HostName"></label>
            </th>
            <th>
                <label asp-for="@Model.Games.FirstOrDefault().HostScore"></label>
            </th>
            <th>
                <label asp-for="@Model.Games.FirstOrDefault().IsPlayoffGame"></label>
            </th>
            <th>
                <label asp-for="@Model.Games.FirstOrDefault().Notes"></label>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var game in Model.Games)
    {
        <tr>
            <td>
                @game.SeasonId
            </td>
            <td>
                @game.Week
            </td>
            <td>
                @game.GuestName
            </td>
            <td>
                @game.GuestScore
            </td>
            <td>
                @game.HostName
            </td>
            <td>
                @game.HostScore
            </td>
            <td>
                @game.IsPlayoffGame
            </td>
            <td>
                @game.Notes
            </td>
            <td>
                <a asp-action="Details" asp-route-id="@game.ID" class="btn btn-primary">Details</a>
                <a asp-action="Edit" asp-route-id="@game.ID" class="btn btn-primary">Edit</a>
                <a asp-action="Delete" asp-route-id="@game.ID" class="btn btn-primary">Delete</a>
            </td>
        </tr>
    }
    </tbody>
</table>
